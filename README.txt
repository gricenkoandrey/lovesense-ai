LoveSenseAI v6 — Replit-ready (conversion-optimized)

What's in v6:
- Trial mode: each user gets 2 free AI requests.
- Fast Checkout (demo) — one-click simulated purchase to maximize conversions.
- Referral reward: referrer receives 7 days Premium after referred user's paid purchase.
- Admin panel: improved UI and marketing templates optimized for Threads.
- Optional integration with OpenAI (set OPENAI_API_KEY in Replit Secrets) for real AI responses.
- Optional Stripe integration (set STRIPE_SECRET) for real payments (requires webhooks/public endpoint).